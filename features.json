[
  {
    "category": "functional",
    "description": "Script displays help text when -h or --help flag is provided",
    "plan_ref": "Task 1 - Add CLI argument parsing for -n/--num flag",
    "steps": [
      "Step 1: Run ./parallel-impl.sh -h",
      "Step 2: Verify help text is displayed including usage, options, and examples",
      "Step 3: Verify script exits with code 0",
      "Step 4: Run ./parallel-impl.sh --help and verify same output"
    ],
    "passes": true
  },
  {
    "category": "functional",
    "description": "Script accepts -n flag to set number of implementations",
    "plan_ref": "Task 1 - Add CLI argument parsing for -n/--num flag",
    "steps": [
      "Step 1: Run bash -n parallel-impl.sh to verify no syntax errors",
      "Step 2: Create a mock test by checking NUM_IMPLEMENTATIONS variable after parsing",
      "Step 3: Verify -n 2 sets NUM_IMPLEMENTATIONS to 2",
      "Step 4: Verify --num 5 sets NUM_IMPLEMENTATIONS to 5"
    ],
    "passes": true
  },
  {
    "category": "functional",
    "description": "Script maintains backwards compatibility with positional argument",
    "plan_ref": "Task 1 - Add CLI argument parsing for -n/--num flag",
    "steps": [
      "Step 1: Run ./parallel-impl.sh 'test feature' without any flags",
      "Step 2: Verify NUM_IMPLEMENTATIONS defaults to 3",
      "Step 3: Verify FEATURE_REQUEST is set to 'test feature'"
    ],
    "passes": true
  },
  {
    "category": "functional",
    "description": "Script validates numeric input for -n flag",
    "plan_ref": "Task 1 - Add CLI argument parsing for -n/--num flag",
    "steps": [
      "Step 1: Run ./parallel-impl.sh -n abc 'test' and verify error message",
      "Step 2: Run ./parallel-impl.sh -n 0 'test' and verify error (out of range)",
      "Step 3: Run ./parallel-impl.sh -n 15 'test' and verify error (out of range)",
      "Step 4: Verify error messages are clear and include valid range"
    ],
    "passes": true
  },
  {
    "category": "functional",
    "description": "Script shows error when feature request is missing",
    "plan_ref": "Task 1 - Add CLI argument parsing for -n/--num flag",
    "steps": [
      "Step 1: Run ./parallel-impl.sh -n 2 (without feature request)",
      "Step 2: Verify error message about missing feature request",
      "Step 3: Verify script exits with non-zero code"
    ],
    "passes": true
  },
  {
    "category": "functional",
    "description": "README documents the -n/--num flag",
    "plan_ref": "Task 2 - Update documentation",
    "steps": [
      "Step 1: Read README.md",
      "Step 2: Verify -n or --num flag is documented in Usage section",
      "Step 3: Verify example usage includes the flag",
      "Step 4: Verify Configuration section mentions CLI flag as preferred method"
    ],
    "passes": true
  }
]
